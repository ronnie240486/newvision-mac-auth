# Optimus — build sem patch progressivo

Esta build foi reconstruída a partir da árvore frosted original. As coroutines originais de `MoviesViewModel` e `SeriesViewModel` foram preservadas; não existe o patch que emitia `move-object v8, v14` e causava `VerifyError` na criação de Séries.

Não foram aplicadas alterações novas de sessão, player ou visual nesta etapa.

SHA-256: `21e620df019791b289b34fb0c54c93ec022b25c136558955b3c0bdaa63764331`
